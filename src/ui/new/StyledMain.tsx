import styled from "styled-components"

export const StyledMain = styled.main`
     width: 1000px;
     padding 60px 40px;

     background-color: var(--primary-background);
     border-radius: 20px;
     box-shadow: 0px 13px 10px 10px rgba(0, 0, 0, 0.05);
`